# Use a project-local npm cache to avoid permission issues with the global ~/.npm directory
cache=.npm-cache
# Ensure npm uses a writable tmp directory within the project as well
# The next line is supported by npm v9+: set a temp directory
# If your npm is older, it will ignore this key harmlessly
cache-min=0
prefer-offline=false
progress=true
fund=false
audit=false
