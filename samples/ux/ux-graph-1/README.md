# Three.js: сравнение роста функций

График сравнивает три функции по оси Y в масштабе `log10(f(N))`:
- `k^N`
- `N!`
- `2^{N^2}`

Запуск: откройте `index.html`. Для TypeScript исходник в `src/main.ts`; соберите в `dist/main.js`.
